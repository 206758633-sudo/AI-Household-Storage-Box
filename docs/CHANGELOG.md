# 变更日志

## 2026-06-08

- 修复底部输入条不可感知的问题，新增独立文字输入行，并保留语音输入按钮。
- 补齐资产详情信息：购入价格、日均成本、购买时间、分类、使用状态。
- 修复详情页编辑按钮无法编辑的问题，新增本地与云端 `update_entry` 更新链路。
- 修复本地测试时仍初始化云开发导致启动超时的问题，默认启用显式本地测试模式。
- 优化归档速度：新增记录先用本地规则快速入库，AI 改为后台复核。
- 修复开发者工具云函数超时阻塞测试的问题，新增本地测试存储兜底。
- 新增通用 `LLM_*` 环境变量，兼容替换不同 OpenAI 格式模型。
- 新增微信小程序前端骨架与首页主流程。
- 新增微信云开发云函数 `storage_box_api`。
- 新增腾讯云混元 AI 归档调用与本地规则兜底。
- 新增云数据库 `entries` 数据模型文档。
- 新增业务规则测试。
